---
message_bus_servers:
<% p('nats.machines').each do |ip| %>
  - nats://<%= p('nats.user') %>:<%= p('nats.password') %>@<%= ip %>:<%= p('nats.port') %>
<% end %>
external_host: <%= p('external_host') %>
external_ip: <%= spec.networks.send(p('networks.riak_cs_network')).ip %>
port: <%= p('riak_cs.port', '8080') %>
