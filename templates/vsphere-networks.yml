networks:
- name: riak-cs1
  subnets:
  - range: (( merge ))
    gateway: (( merge ))
    dns: (( merge ))
    static: (( merge ))
    reserved: (( merge )) #IPs that get dynamically allocated for this release by bosh are guaranteed to be outside the reserved IP ranges.
    cloud_properties:
      name: (( merge ))
