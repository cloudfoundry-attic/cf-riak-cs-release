networks:
- name: riak-cs-network
  type: manual
  subnets:
  - name: riak-cs-subnet
    range: ((merge))
    reserved: ((merge))
    static: ((merge))
    gateway: ((merge))
    dns: ((merge))
    cloud_properties:
      security_groups:
      - cf
      subnet: (( merge ))
