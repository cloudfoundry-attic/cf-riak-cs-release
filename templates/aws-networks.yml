networks:
- name: riak-cs1
  type: manual
  subnets:
  - name: default_unused
    range: ((merge))
    reserved: ((merge))
    static: ((merge))
    gateway: ((merge))
    dns: ((merge))
    cloud_properties:
      security_groups:
      - <<: (( merge ))
      subnet: (( merge ))
